# 服务器 HTTP 端口
server.port=8888

# JDBC 配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/hantest?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useSSL=true
spring.datasource.username=root
spring.datasource.password=han123

# MyBatis 映射器文件的位置
mybatis.mapper-locations=classpath:com/han/mapper/*.xml

# 上传文件总的最大值
spring.servlet.multipart.max-request-size=10MB

# 单个文件的最大值
spring.servlet.multipart.max-file-size=10MB
